# AstraOS GRUB Configuration
# Timeout 0 for fast boot during development; increase for multi-boot setups

set timeout=3
set default=0

menuentry "AstraOS - AstraCore Kernel" {
    multiboot /boot/astra.kernel
    boot
}
